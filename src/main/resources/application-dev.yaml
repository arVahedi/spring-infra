spring:
  flyway:
    locations: classpath:db/migration,classpath:/db/testdata
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true  # Format SQL nicely
        highlight_sql: true  # Syntax highlighting (console only)
        use_sql_comments: true  # Add helpful comments

# security:
#   idp:
#     module: inMemoryIdentityProviderModule
#     in-memory:
#       credentials:
#         - "adminmem;$argon2id$v=19$m=4096,t=3,p=1$QnVwdGpUVmR3OUtHV29Pdw$QDBYFDfx9ePp6PwjP95woA;USER_MANAGEMENT_AUTHORITY;ACCOUNT_INFO_AUTHORITY"
#         - "usermem;$argon2id$v=19$m=4096,t=3,p=1$QnVwdGpUVmR3OUtHV29Pdw$QDBYFDfx9ePp6PwjP95woA;ACCOUNT_INFO_AUTHORITY"

springdoc:
  api-docs:
    enabled: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.stat: DEBUG
